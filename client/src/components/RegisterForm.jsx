import { useState } from "react"

export default function RegisterForm({username, password, handleRegisterSubmit, handleUsernameInput, handlePasswordInput}){

    return(
        <p>In progress</p>
    )
}